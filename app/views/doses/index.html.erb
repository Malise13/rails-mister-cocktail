<% @doses.each do |dose| %>
  <h2><%= dose.name %></h2>
<% end %>

<%= link_to "Delete", dose_path(dose),
      method: :delete,
      data: { confirm: "Are you sure you don't want to drink this?" } %>

<% @doses.each do |dose| %>
  <%= link_to dose.name, dose_path(cocktail) %>
<% end %>
